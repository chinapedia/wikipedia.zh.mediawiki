{{unreferenced|time=2013-10-15T10:36:48+00:00}}
{{NoteTA|G1=Communication}}<br />
{| class="wikitable" style="float: right; text-align: center; margin: 1em;"
|-
! rowspan="2" | 7位数据<br/>（1的个数）
! colspan="2" | 带有校验位的字节
|- 
! 偶校驗位
! 奇校驗位
|-
| 0000000（0）
| 0000000'''0'''
| 0000000'''1'''
|- 
| 1010001（3）
| 1010001'''1'''
| 1010001'''0'''
|- 
| 1101001（4）
| 1101001'''0'''
| 1101001'''1'''
|- 
| 1111111（7）
| 1111111'''1'''
| 1111111'''0'''
|}
'''奇偶校验位'''（{{lang-en|'''parity bit'''}}）或'''校验比特'''（{{lang-en|'''check bit'''}}）是一个表示给定位数的二进制数中1的个数是[[奇数|奇数]]还是[[偶数|偶数]]的[[二进制|二进制数]]。奇偶校验位是最简单的[[错误检测与校正|错误检测码]]。

奇偶校验位有两种类型：'''偶校验位'''与'''奇校验位'''。

以'''偶校验位'''來說，如果一组给定数据位中1的个数是奇数，補一個bit为1，使得总的1的个数是偶数。例：0000001, 補一個bit为1, 0000001'''1。'''

以'''奇校验位'''來說，如果给定一组数据位中1的个数是奇数，補一個bit为0，使得总的1的个数是奇数。例：0000001, 補一個bit为0, 0000001'''0。'''

偶校验实际上是[[循环冗余校验|循环冗余校验]]的一个特例，通过[[多项式|多项式]] ''x'' + 1 得到1位[[循環冗餘校驗|CRC]]。

==錯誤檢測==

如果[[傳輸|傳輸]]過程中包括校驗位在內的奇數個數據位發生改變，那麼奇偶校驗位將出錯表示傳輸過程有錯誤發生。因此，奇偶校驗位是一種錯誤檢測碼，但是由於沒有辦法確定哪一位出錯，所以它不能進行錯誤校正。發生錯誤時必須扔掉全部的數據，然後從頭開始傳輸數據。在噪聲很多的媒介上成功傳輸數據可能要花費很長的時間，甚至根本無法實現。但是奇偶校驗位也有它的優點，它是使用一位數據能夠達到的最好的校驗碼，並且它僅僅需要一些[[异或门|异或门]]就能夠生成。參見[[漢明碼|漢明碼]]中關於其它錯誤校正碼的描述。

==使用==

由于它很简单，所以奇偶校验位用于许多计算机[[硬件|硬件]]中，遇到麻烦时能够重新操作或者通过简单的错误检测就能起到很大作用的场合。例如[[SCSI|SCSI]]总线使用奇偶校验位检测传输错误，许多[[微处理器|微处理器]]的指令[[高速缓存|高速缓存]]中也包括奇偶校验位保护。因为指令缓存数据是[[主内存|主内存]]数据的副本，所以在发现错误的时候能够抛弃错误数据并且重新取回数据。

在[[串行通信|串行通信]]中，常用的格式是7个数据位、1个校验位、1到2个停止位。这种格式用方便的8位字节巧妙地适应了所有的7位[[ASCII|ASCII]]字符。也可以用其它的格式表示，8位数据加上1个校验位可以传输任意的8位字节数据。

在串行通信中，奇偶校验位通常是由[[UART|UART]]这样的接口硬件生成、校验的，在接收方，通过接口硬件中的[[寄存器|寄存器]]的状态位传给CPU以及[[操作系统|操作系统]]。错误数据的恢复通常是通过重新发送数据，这个过程通常由如操作系统输入输出程序这样的软件处理的。

===奇偶校验块===

一些[[冗余磁盘阵列|冗余磁盘阵列]]（RAID）使用奇偶校验块实现冗余。如果阵列中的一块磁盘出现故障，工作磁盘中的数据块与奇偶校验块一起来重建丢失的数据。

下面的图表每列表示一个磁盘，假设A1 = 00000111、A2 = 00000101以及A3 = 00000000。A1、A2、A3 [[异或|异或]]得到的Ap等于00000010。如果第二个磁盘出现故障，A2将不能被访问，但是可以通过A1、A3与Ap的异或进行重建：

A1 XOR A3 XOR Ap = 00000101

       冗余磁盘阵列
  A1        A2        A3
  Ap        B1        B2
  Bp        C1        C2
  C3        C4        Cp

 注意：数据块是格式A#，奇偶校验块是Ap。

[[Category:电信|Category:电信]]
[[Category:二进制算术|Category:二进制算术]]
[[Category:错误检测与校正|Category:错误检测与校正]]
[[Category:數字電路|Category:數字電路]]