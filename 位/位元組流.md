{{NoteTA
| G1 = IT
}}
在[[電腦科學|電腦科學]]裡面，'''位元組流'''（byte stream）是一種[[位元流|比特流]]，不過裡面的[[位元|比特]]被打包成一個個我們叫做[[位元組|位元組]]（Bytes）的單位。

在[[電腦網路|電腦網路]]內，'''八[[位元|比特]]字串流'''（octet stream）有時會拿來代表同樣的東西；這種措辭強調了我們的位元組是8[[位元|比特]]的位元組，或者說[[八位元|八比特]]（octets）。最早位元組這個詞被引進的時候，本身並沒有一個大小的標準；不過大多數現在的電腦位元組都等同於八位元。

正式的說，一個位元組流是一種特定的[[抽象化|抽象化]]，一個讓實體（entity）可以傳輸一系列的位元組給處在另一端實體的一種[[通信|通信]]頻道。一般來說這種頻道會是雙向，不過有時有單向的。在幾乎所有的狀況，這裡的頻道都具有所謂''可靠的''特質;也就是，在另一端會按照正確的順序出現應該出現的位元組（現實生活中有些頻道，有時會順序錯誤，有時會多出或者失去一些位元組）。

比較不正式的說，我們可以把它想做是兩個實體之間的[[電路管線|管線]]（conduit）；其中一個實體會將位元組輸入管線，另一個實體則接收這些位元組。這個管線可以是短暫或者永久的。

==範例==
在多數的[[作業系統|作業系統]]，包含[[類Unix系統|類Unix系統]]和[[Microsoft_Windows|Windows]]，一個行程（process）想要去取得任何[[電腦檔案|檔案]]都是一種位元組流的範例。特別是每個行程都有的三個[[標準串流|標準串流]]（stdin, stdout, stderr），這三個位元組流可視為是單向位元組流的範例。UNIX裡面[[管線_(Unix)|pipe]]的機制常被使用於連接不同的行程，並且用來創造行程之間的位元組流。

另外一個在[[網路傳輸協議|網路傳輸協議]]裡面比較有名，且會提供位元組流給[[客戶端|客戶端]]的範例是[[TCP/IP協議|TCP/IP通訊協定]]裡面的[[傳輸控制協議|傳輸控制協議]]（TCP），這種協議提供了雙向的位元組流。

對於任意位元組流的[[互聯網媒體類型|網際網路媒體型式]]是<tt>application/octet-stream</tt>。其他媒體型式則會以公開的格式來定義他們自己的位元組流。

==相關條目==
* [[字串流|字串流]]
* [[位元流|位元流]]
* [[Flow_(computer_networking)|Flow (computer networking)]]
* [[可靠位元組流|可靠位元組流]]
* [[Byte_orientation|Byte orientation]]
* [[Byte-oriented_protocol|Byte-oriented protocol]]
* [[Byte_Stream_Protocol|Byte Stream Protocol]]

[[Category:資料傳輸|Category:資料傳輸]]
[[Category:電腦網路|Category:電腦網路]]