{{Documentation subpage}}
<!-- PLEASE ADD CATEGORIES AT THE BOTTOM OF THIS PAGE -->
{{esoteric}}

=== 用途 ===

Provides an easy way to load data into the [[Template:Coord|Coord]] template when building [[Wikipedia:WikiProject Infoboxes|Infoboxes]] that require direct data input for use in maps. The data the Coord template uses is loaded in that template without additional effort. The new template will behave as if Coord template where used directly. Because the Coord template is used all the functionality that template offers such as the creation of [[hCard]]s are implemented.

=== 用法 ===

{| class="wikitable" style="line-height: 1.0 font-size: 90%"
! Field    !! Description
|-
| <code>lat_d</code>   = || Degrees of latitude (can be a [[decimal degrees|decimal degree]]).
|-
| <code>lat_m</code>   = || Minutes of latitude.
|-
| <code>lat_s</code>   = || Seconds of latitude.
|-
| <code>lat_NS</code>  = || N for north or S for south of the equator.
|-
| <code>long_d</code>  = || Degrees of longitude (can be a [[decimal degrees|decimal degree]]).
|-
| <code>long_m</code>  = || Minutes of longitude.
|-
| <code>long_s</code>  = || Seconds of longitude.
|-
| <code>long_EW</code> = || E for east or W for west of the prime meridian.
|-
| <code>type</code>    = || For information about <code>type</code> go [[Wikipedia:WikiProject Geographical coordinates#type:T|here]].
|-
| <code>pop</code>     = || For use with <code>type:city</code> and will produce <code>city(pop)</code>
|-
| <code>region</code>  = || For information about <code>region</code> go [[Wikipedia:WikiProject Geographical coordinates#region:R|here]].
|-
| <code>scale</code>   = || For information about <code>scale</code> go [[Wikipedia:WikiProject Geographical coordinates#scale:N|here]].
|-
| <code>source</code>  = || For information about <code>source</code> go [[Wikipedia:WikiProject Geographical coordinates#source:S|here]].
|-
| <code>globe</code>   = || For information about <code>globe</code> go [[Wikipedia:WikiProject Geographical coordinates#globe:G:S|here]].
|-
| <code>display</code> = || <code>inline</code> or <code>inline,title</code> or <code>title</code> and variants.
|-
| <code>format</code>  = || <code>dms</code> for degree, minutes, second output, <code>dec</code> for decimal digit output.
|-
| <code>name</code>    = || For information about <code>name</code> go [[Wikipedia:WikiProject Geographical coordinates#name|here]].
|}

Degrees must be specified. If degrees are not signed then NS or EW must also be specified. If <code>type</code> is <code>city</code> and <code>pop</code> is the population then <code>city(population)</code> is loaded in the Coord template.

For advice on creating new templates that include coordinate data see [[Wikipedia:WikiProject Geographical coordinates#Creating new templates|Creating new templates]].

==== 空白模板 ====

<pre>
{{Infobox coord
| lat_d   = 
| lat_m   = 
| lat_s   = 
| lat_NS  = 
| long_d  = 
| long_m  = 
| long_s  = 
| long_EW = 
| type    = 
| pop     = 
| region  = 
| scale   = 
| source  = 
| globe   = 
| display = 
| format  = 
| name    = 
}}
</pre>

== 参见 ==

* [[Template:Coord]]
* [[Wikipedia:WikiProject Infoboxes|WikiProject Infoboxes]]
* [[Wikipedia:WikiProject Geographical coordinates|WikiProject Geographical coordinates]]

<includeonly>
<!-- CATEGORIES HERE, THANKS -->
{{DEFAULTSORT:{{PAGENAME}}}}
[[Category:地理信息框模板|标]]

</includeonly>