<includeonly><!-- 在這裡加入模板的保護標識 --></includeonly><noinclude>{{Documentation subpage}}</noinclude>
<!-- 在本行下編輯模板說明 -->
{{tl|start tab}}可在維基計劃或其他非條目頁面頂端產生導航用的標籤列。請勿在條目文章中使用本模板。

== 使用方法 ==
<pre>
{{Start tab
| freeform-n          =
| link-n              =
| link2-n             =
| link3-n             =
| link4-n             =
| link5-n             =
| tab-n               =
| frame               = 
| border              = 
| off tab color       = 
| on tab color        = 
| rounding            = 
| tab alignment       = 
| image-              = 
| tab font-size       = 
| nowrap              = 
| tab spacing percent = 
| preums              = 
}}
</pre>

== 參數 ==
; <code>freeform-n</code>: 使用 <code>freeform-n = yes</code> 可讓 <code>tab-n</code> 參數中的數值成為純文字和連結以外的內容，其中「n」代表標籤編號，最多可支援20個。（請見下方說明）。
; <code>link-n</code>: 標籤的連結目標位置，其中「n」代表標籤編號，最多可支援20個。
; <code>link2-n</code>: 標籤的第2個連結目標位置，其中「n」代表標籤編號，最多可支援20個（僅限在 <code>freeform-n</code> 設為 <code>yes</code> 時可使用）。
; <code>link3-n</code>: 標籤的第3個連結目標位置，其中「n」代表標籤編號，最多可支援20個（僅限在 <code>freeform-n</code> 設為 <code>yes</code> 時可使用）。
; <code>link4-n</code>: 標籤的第4個連結目標位置，其中「n」代表標籤編號，最多可支援20個（僅限在 <code>freeform-n</code> 設為 <code>yes</code> 時可使用）。
; <code>link5-n</code>: 標籤的第5個連結目標位置，其中「n」代表標籤編號，最多可支援20個（僅限在 <code>freeform-n</code> 設為 <code>yes</code> 時可使用）。
; <code>tab-n</code>: 標籤名稱，其中「n」代表標籤編號，最多可支援20個。

* 標籤的數量上限為20。
* 若要使用「下一頁」功能，請在頁面結尾處使用 {{tl|end tab}} 模板。

每個標籤都是由以下兩個參數組成：一個 <code>link</code> 參數，指定該標籤的連結目標位置；以及一個 <code>tab</code> 參數，用來指定該標籤上顯示的文字。如果 <code>link</code> 參數所指向的頁面與模板所在的頁面相同，該標籤的樣式會有所變化。<code>freeform</code> 參數可用在任何一個標籤上（例如 <code>|freeform-2=yes</code> 可產生一個包含多個內部和外部連結的標籤）。

額外參數：

; <code>frame</code>: 設為 <code>yes</code> 時可將標簽下方的頁面內容加入外框。可在頁面底端加入 <code><nowiki></div></div></nowiki></code> 來結束外框。
; <code>border</code>: 外框格式。
; <code>off tab color</code>: 非選擇中的標籤顏色。
; <code>on tab color</code>: 選擇中的標籤顏色。
; <code>rounding</code>: 在標籤角落加入圓角效果。
; <code>tab alignment</code>: 指定標籤標題的對齊方式（左齊：left、置中：center、右齊：right）。
; <code>image-(N°)</code>: 可在標籤標題旁加入圖片（寬度設定為 20px）
; <code>tab font-size</code>: 指定標籤文字大小。
; <code>nowrap</code>: 避免標籤中較長的文字自動換行。
; <code>tab spacing percent</code>: 指定標籤之間的間距。
; <code>preums</code>: '''僅供'''在 {{tl|Tabs}} 中使用。

== 範例 ==
=== 基本範例 ===
<pre>
{{ start tab
| tab-1  = 當前頁面
| link-1 = Template:Start tab
| tab-2  = 連結一
| link-2 = Portal:首頁
| tab-3  = 連結二
| link-3 = Portal:首頁/TODO
| tab-4  = 連結三
| link-4 = Portal:首頁/列表
}}

此處為頁面文字
</pre>

{{ start tab
| tab-1  = 當前頁面
| link-1 = Template:Start tab
| tab-2  = 連結一
| link-2 = Portal:首頁
| tab-3  = 連結二
| link-3 = Portal:首頁/TODO
| tab-4  = 連結三
| link-4 = Portal:首頁/列表
}}
此處為頁面文字

=== 自定格式範例 ===
<pre>
{{ start tab
| tab-1  = 當前頁面
| link-1 = Template:Start tab
| tab-2  = 連結一
| link-2 = Portal:首頁
| tab-3  = 連結二
| link-3 = Portal:首頁/TODO
| tab-4  = 連結三
| link-4 = Portal:首頁/列表
| border = 1px solid #808080
| off tab color = #f0f0ff
| on tab color = 
| rounding = 3em
| tab alignment = center
}}

此處為頁面文字
</pre>

{{ start tab
| tab-1  = 當前頁面
| link-1 = Template:Start tab
| tab-2  = 連結一
| link-2 = Portal:首頁
| tab-3  = 連結二
| link-3 = Portal:首頁/TODO
| tab-4  = 連結三
| link-4 = Portal:首頁/列表
| border = 1px solid #808080
| off tab color = #f0f0ff
| on tab color = 
| rounding = 3em
| tab alignment = center
}}

此處為頁面文字

== 參見 ==
* {{tl|End tab}}
* {{tl|Tab}}
* {{tl|PageTabs}}
<includeonly>
<!-- 本行下加入模板的分類 -->
[[Category:維基標籤導航模板]]
</includeonly>